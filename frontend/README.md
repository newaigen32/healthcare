# Frontend

The Next.js application for Knowledge Assistant.

From this directory:

```bash
cp .env.local.example .env.local
npm install
npm run dev
```

The app expects the FastAPI backend at `NEXT_PUBLIC_API_URL` (default `http://localhost:8000`).
